/**
 * Login — OTP UX: mobile chip, resend cooldown ring (2m), code validity (5m),
 * segmented code + auto-submit on complete, SoftRing hydrate (UI-06).
 * Fixed: controlled identifier field.
 */

"use client";

export { default } from "./page-impl";
