/**
 * RESTORED — run agent again to re-apply single-site UX if needed
 * Temporary minimal to unblock; full content follows in same path via second push
 */
"use client";
export { DepartmentsListPage } from "./departments-list-impl";
