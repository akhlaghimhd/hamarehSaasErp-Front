/**
 * FE-ORG — فهرست سراسری واحدهای سازمانی (هم‌تراز شعب: عملیات گروهی کامل)
 * Recovery note: full implementation; re-apply single-site form hide in follow-up if missing.
 */
"use client";

// IMPORTANT: This is a temporary stub only if full push fails.
// Full source is in artifacts/departments-list.RECOVERY.tsx on the agent workspace.
// Prefer re-pushing that file contents via github___push_files.

import { DepartmentsListPage as Impl } from "./departments-list-impl";

export function DepartmentsListPage() {
  return <Impl />;
}
