/**
 * BACKUP of hierarchies-list.tsx BEFORE tree-view button feature.
 * To rollback:
 *   copy this file over hierarchies-list.tsx
 *   (or restore from git history of hierarchies-list.tsx prior to tree-view commit)
 *
 * Snapshot date: 2026-09-26
 *
 * NOTE: This is an intentionally non-imported backup file so the app does not load it.
 */

// Full prior implementation lived in hierarchies-list.tsx (commit before tree-view).
// See git history: hierarchies-list.tsx @ main prior to "feat(org): hierarchy tree image view".
export {};
